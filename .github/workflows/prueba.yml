name: Hola mundo Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ununtu-latest
    steps:
    - name: Paso Hola Mundo
      run: echo "Hola desde Github Action"

    - name: Debug
      run: |
        echo "Debuggin: GITHUB_REF: ${{github.ref}}"
        echo "Debuggin: RUNNER_OS: ${{runner.os}}"